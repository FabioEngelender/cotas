rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow everything for debugging - REMOVE BEFORE PRODUCTION
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
